const add = (first, second) => {
    return first + second;
}

const multiply = (first, second) => {
    return first * second;
}

const divideTheFirstNumberByTheSecondNumber = (first, second) => {
    return first / second;
}

export { add, divideTheFirstNumberByTheSecondNumber, multiply };


// export default add;


